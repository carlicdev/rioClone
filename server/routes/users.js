const { Router } = require('express');
const router = Router();

router.get('/', (req, res) => res.send('hello world'));

// Registrar usuario

// Logear usuario

// Obtener usuario

module.exports = router;